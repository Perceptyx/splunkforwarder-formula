### THIS FILE IS AUTOMATICALLY GENERATED by salt

{% set global_monitor = salt['pillar.get']('splunkforwarder:system:inputs:monitor', {}) -%}

{% for id, settings in global_monitor.items()|sort -%}
[monitor://{{ settings['monitor'] }}]
{% for n, v in settings.items()|sort -%}{% if n != 'monitor' -%}
{{ n }} = {{ v }}
{% endif %}{%- endfor %}
{%- endfor %}
