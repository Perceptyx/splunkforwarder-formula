{% set props = salt['pillar.get']('splunkforwarder:props', {}) %}

### THIS FILE IS AUTOMATICALLY GENERATED by salt

{% for id, settings in props.items()|sort -%}
[{{ id }}]
{% for n, v in settings.items()|sort -%}
{{ n }} = {{ v }}
{% endfor %}

{% endfor %}
